<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Personalized feed (M16)

`/feed` (+ `/feed/interests`) is the logged-in port of the mobile app's M13 Home Feed. Design doc: `StreamHub/docs/Epics/Epic Milestone 16.md`. Key facts:

- **Auth-gated, never cached**: both pages are `force-dynamic`, `robots: noindex`, and follow the `/favorites` convention (`getUser()` gate). Website auth is DORMANT but fully prepared behind a build-time flag — see the "Auth activation flag" section below. The root layout MUST stay static — never read `cookies()`/auth there (the flag is safe: a build-time constant).
- **Layering in `lib/feed/`**: `logic.ts` + `transforms.ts` are pure (unit-tested via `npm run test`, vitest); `service.ts`/`interests.ts`/`preferences.ts` take a `SupabaseClient` (server or browser — same functions serve SSR and client refresh); `loadFeed.ts` orchestrates 6 parallel error-isolated section fetches; `events.ts` is the client-only feed_events queue (30s flush, 50-cap, drop-on-failure, keepalive POST straight to PostgREST so leave-flushes survive `pagehide`).
- **Data comes from Supabase directly** (RPCs `compute_user_interest_profile`, `recommend_featured_streamers`, `fetch_feed_recent_streams`, `fetch_trending_categories` + tables `stream_slots`, `stream_clips`, `ai_predictions`, `user_interests`, `user_feed_preferences`, `feed_events`), NOT from the Partner API. The RPCs are granted to `authenticated` only and use `auth.uid()` — they must run under the user's session. Pass the interest profile back to `recommend_featured_streamers` verbatim (snake_case).
- **Watermark cookie** `st_feed_seen` (ISO timestamp, 30d, written client-side on leave): the server computes `since = max(now − 24h, cookie)` for "New for you"; the client keeps that value in a ref so in-session refreshes never shrink the section.
- **Constants in `lib/feed/constants.ts` mirror the app** (`useHomeFeed.ts`) — keep them in sync. Deviations from the app are deliberate and documented in the epic: `streamers.is_hidden` filtering, cancelled-slot exclusion from Up Next, no 24h interest-profile cache (recomputed per request).
- **Feed UX round (2026-07-22, website-only — no app counterpart):** invariants an agent must not accidentally undo:
  - Horizontal rails do NOT own overflow scrolling — `RailScroller` (edge fades + mouse-only chevrons) wraps them; the inner `<ul>`s are plain flex rows.
  - `UpNextMeta` deliberately renders NO reasoning teaser — `SlotCard` shows it inside the card; re-adding it reintroduces the duplicate it fixed. It carries relative start time (post-mount only), the reliability pill, and the `.ics` export (`lib/feed/ics.ts`, RFC 5545, unit-tested).
  - Dismiss events are logged DELAYED: 5s undo window (snackbar), committed early on page leave — undone dismisses must never reach `feed_events` (they'd feed the Phase-4 rank suppression).
  - Inline info cards are capped at 3 per view (`inlineInfoCandidates` in `FeedClient`); the briefing overlay keeps the full digest.
  - Anything timezone/clock-dependent (Up Next day groups, countdowns, "updated ago") renders post-mount only (`mounted` gate) — never emit it from the server.
  - Trending-game tiles navigate to internal `/game/<slug>` when `gameHubSlugs` (server-resolved in `feed/page.tsx` via ISR-cached `listGames`, degrades to `{}`) has the game; fallbacks: chip filter → Twitch directory.
  - `toPublicStreamSlot` maps `viewer_count` only for live slots with a <25min-fresh sample (partner-API contract) — the live rail's "watching" badge depends on it.
  - Lightbox playlist nav (prev/next + arrow keys) walks the source rail's embeddable clips; NO autoplay-next (the Twitch embed exposes no reliable ended signal without their player JS API).
- **Local E2E loop** (website auth is dormant, so a dev-only bypass exists):
  1. StreamHub repo: `npx supabase start` && `npx supabase db reset` (seeds feed fixtures).
  2. Create `.env.development.local` (dev-only; prod builds ignore it) pointing at the local stack: `NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321`, `NEXT_PUBLIC_SUPABASE_ANON_KEY=<publishable key from npx supabase status>`, `SUPABASE_SERVICE_ROLE_KEY=<secret key>`, `PARTNER_API_BASE_URL=http://127.0.0.1:54321/functions/v1/partner-api-v1`, `PARTNER_API_KEY=<stk_test_ key from seed.sql>`, `NEXT_PUBLIC_SITE_URL=http://localhost:3000`.
  3. `node scripts/seed-dev-user.mjs` (refuses non-localhost URLs).
  4. `npm run dev` → open `http://localhost:3000/api/dev-login` → lands signed-in on `/feed`. The route is triple-guarded (NODE_ENV=development + localhost Supabase URL + service key present) and 404s in production builds; it signs in via admin `generateLink` + `verifyOtp` because the local auth config has hCaptcha enabled.
  Gotcha: `TaskStop`/Ctrl-C can leave a detached `next dev` holding port 3000 — the next `npm run dev` then errors with "Another next dev server is already running" and prints the PID; `taskkill /PID <pid> /F` it.
- **QA test accounts** (created 2026-07-25 so the auth-gated pages — `/feed`, `/favorites`, `/program`, `/settings`, `/feed/interests`, `/onboarding` — can be exercised as a real signed-in user):
  - *Local*: `feed-tester@local.test` (seeded by `scripts/seed-dev-user.mjs`; favorites shroud+pokimane, interests Just Chatting+VALORANT). Use the dev-login loop above.
  - *Production* (real, email-confirmed users in the live `auth.users`; password `StreamerTest!2026` for both): `qa-feed@streamertimes.tv` = rich (favorites xqc/shroud/jynxzi/ninja + interests → populated feed), `qa-fresh@streamertimes.tv` = empty (onboarding / empty-state). Both tagged `raw_user_meta_data.role = 'qa_test_account'`. Log in manually at `streamertimes.tv/auth/login` (email form is stealth-live; hCaptcha runs in the browser).
  - **Browserless prod render check**: `node scripts/prod-selftest.local.mjs [email]` (gitignored). Prod enforces hCaptcha on password login, so the harness mints a captcha-free session via admin `generateLink`(magiclink)→`verifyOtp`, reproduces the exact `@supabase/ssr` cookies, and fetches each gated page on `streamertimes.tv` (plus a no-cookie control that must 307 → `/auth/login`). Requires `PROD_SUPABASE_SERVICE_ROLE_KEY=<prod service_role / sb_secret_ key>` in `.env.development.local` (gitignored; a DISTINCT var from the local-stack `SUPABASE_SERVICE_ROLE_KEY`, which points at localhost). Rotate/remove that key when you're done — the accounts and browser login are unaffected either way.

# Program page (2026-07-22)

`/program` is the logged-in port of the mobile app's Program tab: a single-day EPG of the user's favorite streamers' stream slots. Same gated-page conventions as `/feed` (force-dynamic, noindex, `signInGateRedirect`, robots disallow, dormant until the auth flag flips). Key facts:

- **Server/client split for timezones**: the server component only gates auth + fetches favorites/slots (reusing `lib/feed/service.ts fetchStreamSlots` + `fetchHiddenStreamerIds`); ALL day/hour grouping runs in the viewer's LOCAL timezone inside `ProgramClient` (`components/web/program/`), which renders a skeleton until mounted — never emit timezone-dependent markup from the server here.
- **Pure logic in `lib/program/logic.ts`** (unit-tested): `buildProgramDay` = status recompute → date filter (9PM-prev-day carry-over, always-on live on all dates, regular live only today) → dedupe → Live Now + hour sections → Missed (real slots ended <6h ago, today only) → Offline today (vs the ACTUAL current day, app parity). Reuses `calculateStreamStatus`/`deduplicateStreamerSlots` from `lib/feed/logic.ts`. **Cancelled slots are KEPT here** — the feed's Up Next exclusion is a feed-only deviation; a regression test guards this.
- **Cancelled slots (Partner API `slot_kind`, 2026-07-22)**: the DTO carries `slot_kind` (`regular | new | cancelled`) on both `PublicStreamSlot` and `PublicPrediction` (StreamHub `partner-dto.ts`, deployed). `SlotCard` derives the greyed treatment + CANCELLED badge from it; `SlotStatusText`/`getStatusText` render the localized "No stream expected (usually around …)" line (`statusNoStreamExpected` in `lib/i18n-slot.ts`, all 12 languages); `/schedule/[id]` hides the watch buttons. The website mirror keeps the field optional (deploy skew).
- **Navigation (UX round 2026-07-23)**: a 7-day strip (`buildStripDays`) is the primary day navigation; the calendar chip (native `<input type="date">` overlay + `showPicker()`, older Safari falls back to focus) covers arbitrary/past dates and lights up only when the selected date is OUTSIDE the strip. The Now/4pm/8pm chips scroll to section anchors (`id="program-<sectionId>"`, `scroll-mt-44`) and their highlight is OWNED by a scroll-spy (position, not selection — `activeChipForSection`); 'Now' additionally resets to today. 5-min auto-refresh + tab-return + manual button (FeedClient pattern), refresh failures keep the current data; a 60s clock tick re-renders derived state only (countdowns, now line, "Updated … ago" — no fetch).
- **UX round 2026-07-23 invariants**: `listFavoriteStreamers` now selects `twitch_login`/`youtube_channel_id` — the live-card watch pills (`buildWatchLinks`) depend on them and render as SIBLINGS of the SlotCard link (never nested anchors). Upcoming cards get a meta row (feed UpNextMeta pattern): `programRelativeStartLabel` (≤12h window, null when >1min overdue) + per-slot `.ics` (reuses `lib/feed/ics.ts`; `slotVeventLines`/`wrapVcalendar` are shared with the whole-day export in `lib/program/ics.ts` — same UIDs, re-imports update). Offline rows show `findNextExpectedSlot`→"Next: Tomorrow ~8pm" (cancelled slots never count as next-expected). The empty day state offers `findNextDayWithStreams`. The now line (`nowLineIndex`) is section-granular and today-only. Section titles are real `h2`s (screen-reader navigation) + `aria-live` feedback on manual refresh. All helpers are pure in `lib/program/logic.ts` (unit-tested).
- Local E2E: same dev-login loop as `/feed` (the seeded user's favorites have slot fixtures).

# Signed-in section switcher (2026-07-25)

`FeedNavTabs` (`components/web/FeedNavTabs.tsx`) is an underline tab strip rendered ABOVE the `<h1>` on the three signed-in pages — `/feed`, `/program`, `/favorites` — so users can jump between them and always see where they are. Key facts:

- **Pure server component**: the active tab is passed in per page (`active="feed" | "program" | "favorites"`) — no client-side `usePathname`, no `'use client'`. Links are locale-aware via `localeHref` so the picked locale survives navigation (`/de/feed` → `/de/program`). Active tab carries `aria-current="page"`; `<nav aria-label>` is localized. Horizontal-scrolls (`-mx-4 overflow-x-auto px-4` + `min-w-max`) instead of wrapping on narrow screens.
- **Labels double as the page `<h1>`** so the active tab always matches the title. They live in the shared chrome lexicon (`lib/i18n-chrome.ts` → `feedNav`: `sections`/`feed`/`program`/`favorites`, all 12 UI languages) — this also localized the three headings, which were English-only before. The feed heading is passed into `FeedClient` via the new optional `title` prop (default `'My Feed'`).
- Each page now resolves `locale` from `params` (`isUiLang(rawLocale) ? rawLocale : 'en'`) — these were the only signed-in pages not already reading the route locale.
- **Not** wired into the header/mobile menu (those already list the three pages in the account dropdown); this is the on-page switcher only. The favorites/program `"N streamers"` count line stays English (pre-existing, out of scope).

# New-user onboarding (2026-07-22)

`/onboarding` is the post-signup wizard (app-onboarding port): choice → import Twitch follows OR pick favorites manually → done. Same gated-page conventions as `/feed` (force-dynamic, noindex, `signInGateRedirect`, robots.txt disallow). Key facts:

- **Entry points — returning users are never redirected here**: (1) the OAuth callback sends FRESH signups (account `created_at` < 5 min, `isFreshSignup` in `lib/onboarding.ts`) to `/onboarding` — Twitch signups directly to `?step=import`; an explicit `?next` survives as the wizard's finish target and a `next` already inside `/onboarding` passes through untouched (the wizard's own "Connect Twitch" round-trip). (2) `/auth/confirm` `type=signup` success defaults to `/onboarding` (`confirmResultRedirect`). (3) The feed's `EmptyFavoritesCard` links to it. All pure helpers live in `lib/onboarding.ts` (unit-tested).
- **Import step reuses the Settings import**: the state machine + phase UI were extracted to `components/web/twitch-import/` (`useTwitchImport` + `TwitchImportPhaseView`); `TwitchImportButton` (Settings) is now a thin modal wrapper around them. Both talk to the `import-twitch-follows` edge function (fetch/import, 20 new streamers/day).
- **Twitch OAuth now requests `user:read:follows`** (`lib/twitch-scopes.ts`, used by the login server action and `lib/web/twitchConnect.ts`). Without it the provider token can't read follows — the import silently loaded ZERO follows before this fix. The callback validates the captured token against `id.twitch.tv/oauth2/validate` (must be a Twitch token WITH that scope) so a Google `provider_token` of a multi-identity account is never stashed as `twitchProviderToken`; on validate network failure it falls back to the old `app_metadata.provider === 'twitch'` heuristic.
- **"Connect Twitch" is a standalone Twitch OAuth token flow — NOT Supabase identity linking** (`lib/web/twitchConnect.ts` → `startTwitchFollowImport`). It runs Twitch's own implicit grant (`id.twitch.tv/oauth2/authorize?response_type=token`, `user:read:follows`) and returns to the page `/auth/twitch-import` (`app/[locale]/auth/twitch-import/`, like `/auth/login` — a `'use client'` body wrapped by the root layout; middleware rewrites the unprefixed URL into the English tree, and the token fragment never reaches the server), which reads the token from the URL fragment client-side, verifies the CSRF `state` we stashed in sessionStorage, and stores it in the same `twitchProviderToken` localStorage slot the OAuth callback uses. `router.replace` bounces back to `?step=import` (token fragment dropped), where `useTwitchImport` auto-loads the follows.
  - **Why not `linkIdentity`/`signInWithOAuth`** (the pre-2026-07-25 approach): `linkIdentity` PERMANENTLY attaches the Twitch identity and fails `422: Identity is already linked to another user` for any Twitch account already tied to another ST account (i.e. anyone who ever used "Continue with Twitch") — so email/Google users could never import, and the error surfaced only as the generic "Connecting to Twitch failed" via the callback's provider-error branch. The token flow needs no identity link, so it works uniformly for email/Google/Twitch-first accounts and changes NOTHING about the ST session.
  - Requires `NEXT_PUBLIC_TWITCH_CLIENT_ID` (same value as the backend `TWITCH_CLIENT_ID` secret) + the `/auth/twitch-import` redirect URI registered in the Twitch dev console (prod + localhost) — see `.env.example`. Unset client ID → import buttons degrade to "pick manually". Pure return logic (`lib/web/twitchImportReturn.ts`) is unit-tested; provider errors / denials come back on `next` as `connect_error` (`access_denied`, `scope_denied`, …) with friendly copy + manual-pick fallback.
- **Pick step**: default grid = most-followed streamers via the Partner API (`listStreamers({ order: 'followers' })`, limit 24 — same source as `/api/search`, `is_hidden` filtered server-side, live badges via `getLiveStreamerIdSet`); the Discover RPC chain (`fetchInterestProfile` + `fetchDiscoverRecommendations`) is only the fallback when the Partner API fails or returns empty — the grid heading follows the source ("Most followed" / "Popular right now"). Plus debounced `/api/search`; cards toggle favorites immediately through `FavoritesProvider` (optimistic, idempotent) — Continue only navigates. Deliberate deviation from the app: skipping does NOT seed featured favorites (the feed has a proper empty state + "Popular right now" fallback).
- **No completion flag** (deviation from the app's AsyncStorage step persistence): auto-entry only ever happens right after signup, so a revisit-by-URL just shows the wizard again — harmless. Step state is URL-mirrored (`?step=`, history.replaceState) so refreshes and the Connect-Twitch round-trip resume the right step.
- Local E2E: dev-login loop + auth flags in `.env.development.local`; real Twitch OAuth is NOT locally testable without a Twitch app + registered localhost redirect URI. After deploying, smoke-test one fresh Twitch signup (lands on import with follows listed), one Google signup (choice → Connect Twitch → follows import onto the Google account), one email signup (confirm link → wizard → Connect Twitch → follows import). Regression to verify the collision fix: sign in with Twitch once (claims the identity), sign out, sign up with a DIFFERENT email, Connect Twitch to the SAME Twitch account → import must now succeed (previously "Connecting to Twitch failed").

# Add-streamer flow (2026-07-25)

Signed-in users can add Twitch/YouTube channels that aren't tracked yet, from `/search` and the onboarding pick step (port of the app's M8 flow; NO backend changes — the StreamHub `search-streamers` + `add-streamer` edge functions are called directly with the user JWT, same fetch pattern as the Twitch import: `Authorization: Bearer <session.access_token>`, no apikey header, CORS `*`).

- Service layer `lib/web/streamerSearch.ts` (client-safe, no React — unit-tested): fetchers, `buildAddParams`/`getResultKey`, `addStreamerErrorInfo` (429 daily limit 20 adds/user/day, follower-gate errorCodes `insufficient_followers`/`subscriber_count_hidden_low_videos`/`channel_not_found`/`follower_check_unavailable`, session expiry → sign-in link), centralized English-only copy in `ADD_STREAMER_COPY`.
- `hooks/useExternalStreamerSearch.ts`: debounced (400ms; 0 on /search) search returning `isNew`-only results (DB matches are already rendered from the Partner API); idle when signed out / auth dormant / query <2 or >50 chars. `hooks/useStreamerAdd.ts`: add+favorite with pending guard, cross-user guard (the add call runs 30–90s: AI discovery, EventSub, history, prediction chain), `alreadyExists` treated as success (idempotent — retry after a dropped response is safe), noHistory notices.
- `/search`: `ExternalResultsSection` island below the DB results and inside `NoResultsState`; signed-out visitors keep the "Get the app" box byte-identical (the island renders it as fallback). Platform follows the existing filter chips; the section is skipped for queries >50 chars (edge-function limit; the page allows 100).
- Onboarding pick step: "Not on Streamer Times yet" sub-grid with a Twitch/YouTube toggle; added streamers merge locally into the picked grid (`addedSuggestions`) because the Partner API behind `/api/search` caches ~60s.
- Gotchas: `search-streamers` has NO per-user rate limit (each authenticated keystroke-pause = one Twitch Search API call — watch quota if usage grows); YouTube search results never carry `isLive`; newly added streamers are immediately visible site-wide (`approved` defaults to true, `is_hidden` false, streamer pages render unknown slugs on first visit).

# Game hub UX round (2026-07-23)

`/game/[slug]` invariants an agent must not accidentally undo:

- **Schedule density**: `GameDaySection` renders high/medium slots as full SlotCards and collapses low-confidence upcoming predictions into compact rows behind a closed `<details data-low-bucket>` (crawlable, no reasoning teasers — LOW copy is the weakest and must not get a full card again). Cancelled slots ALWAYS keep the full card. Split logic is pure in `lib/game-schedule.ts` (unit-tested).
- **Filters without payload duplication**: the schedule stays fully server-rendered; `ScheduleFilters` (client) receives it as `children` and toggles the `hidden` attribute on `li[data-slot]`/`section[data-day]`/`details[data-low-bucket]` markers. Do NOT convert the schedule into client props (200 slots would double into the flight payload). Known accepted staleness: DayNavBar/day-heading counts describe the unfiltered schedule.
- **Per-slot `.ics`**: `SlotIcsButton` reuses `lib/feed/ics.ts` (same UIDs as feed/Program exports) and renders as a SIBLING of the SlotCard link (absolute overlay / row-end) — never a nested interactive. Eligibility: upcoming + not cancelled (`isIcsExportable`).
- **Ranking table** reuses `buildGameRankingRows` (same view model as `/rankings/game/[slug]`): Next-stream cell via `NextStreamTime` deep-links `#day-<utc-date>` only when that day section renders (`renderedDayKeys` guard); Hours/28d column hidden on mobile.
- **Next-stream is category-AGNOSTIC** (fixed 2026-07-25): the "Next stream" column of the ranking table means "when is this streamer next live", NOT "next time they stream THIS game". It is fed by `getNextSlotByStreamer(ranked ids)` (no `category` filter) — NOT the category-filtered `upcomingSlots` used for the schedule/day sections. The old wiring blanked the cell for top-follower streamers whose next predicted stream is a different game (e.g. KaiCenat/xQc on `/game/just-chatting`, and most of `/game/counter-strike`). `buildGameRankingRows` still takes the slots as its `upcomingSlots` arg — just pass the unfiltered next-slot list. `GameRankingRow.nextCategory` carries the predicted game of that next slot (null for `slot_kind='cancelled'`), rendered as a muted line under the time.
- **Heatmap** (`StreamTimesHeatmap` + pure `lib/game-heatmap.ts`): data from `/v1/games?category=&include=hour_histogram` (168 UTC minutes cells, weekday 0 = Monday), fetched failure-isolated in the page loader — degrades to a hidden section against an older API. SSR renders the UTC frame; the viewer-local shift happens post-hydration via `useSyncExternalStore` (never emit local-time markup from the server). Sequential single-hue cyan with sqrt intensity — keep it one hue.
- **Follow this game**: `FollowGameButton` renders NOTHING while auth is dormant or signed out (stealth rule — game pages are public/ISR, the session check is client-side only). Persists to `user_game_follows` via `lib/supabase/gameFollows.ts` (RLS); `/settings` lists follows in `FollowedGamesSection`.
- **Live section budget**: 4 visible live cards, up to 20 more inside a closed `<details>`, tail links to the full ranking. The ItemList JSON-LD still mirrors only the ranking/roster — the live expander must not feed it.
- **OG box art**: `renderOgFrame({ sideImage })` switches to the two-column card; the game OG route fetches box art into a data URI with timeout/size guards and MUST degrade to the text card on any failure (build-abort rule).
- **DayNavBar scroll-spy** (shared with the streamer page): IntersectionObserver band highlights the active day; SSR output renders no active state (hydration stays clean).

# /rankings "Next stream" column (2026-07-25)

- EVERY table under `/rankings` shows a "Next stream" column with the expected game beneath the time: the hub index (`/rankings`, all metric preview tables), the metric leaderboards (`/rankings/<metric>`), and the game rankings (`/rankings/game/<slug>`). The `#1` `RankingSpotlight` card appends `· <category>` too.
- Shared `RankingTable` renders it from a `nextSlots: Map<streamerId, PublicStreamSlot>` prop (built by `getNextSlotByStreamer`, category-agnostic). The hub previously passed NO `nextSlots` → no column; it now fetches once over the deduped union of all section ids. `NextStreamCell` shows `slot.category` under `NextStreamTime`, suppressed for `slot_kind='cancelled'`.
- The category is the NEXT slot's predicted game (per-slot), deliberately distinct from the "Main game" column (28d-dominant category from `top_category`). The two often differ — that's the point.

# Homepage feed rebuild (2026-07-27)

`/` (all 12 locales) was rebuilt from an app-marketing hero into an anonymous feed-style page: Masthead (signed-out only, see below; metadata UNCHANGED — the SEO intent split with `/app` still holds) → Live rail ("Most Watched right now", reworked 2026-07-28) → "Today's lineup" → interrupt card → Trending rail → Clips of the week → Quick facts → Risers | Most-streamed duo → Most watched → Discover grid → app end cap → PopularStreamersFooter. Components live in `components/web/home/`; pure view-model helpers in `lib/home/logic.ts` (unit-tested). `HomeHero`/`UpcomingGrid`/`ApiPromo` were deleted. Invariants:

- **The page stays static ISR 60s (K1)**: no `cookies()`/auth in the server component; ALL personalization is client-side (`SignedOutOnly` via `useAuth` behind a `useSyncExternalStore` mounted gate; favorite hearts/bells are client islands). The section fetches run through `Promise.allSettled` — every section degrades to hidden on failure; the page must never throw during prerender (build-abort rule).
- **Fetch-URL timestamp bucketing is MANDATORY** (build incident 2026-07-27): every fetch that embeds a timestamp floors it — `floorToBucket` (5 min, the `next-streams.ts` convention) for slot queries, `floorToHourIso` for slow-moving anon-REST queries. A raw `new Date().toISOString()` gives each of the 12 locale prerenders its own uncached API sweep; that load timed out the sitemap's sequential sweep (which deliberately rethrows) and aborted the deploy. `getLiveStreamerIdSet` + `fetchLiveSlots` bucket internally now.
- **anon-REST + ISR data path** (`lib/server/anon-rest.ts`, generalized from `trending.ts`): plain `fetch` against PostgREST with the anon key + `next:{revalidate}`, 2 attempts. Only anon-RLS-readable tables (verified: `stream_clips`, `ai_predictions`, `stream_viewer_samples`, `streamer_schedule_reliability`, `streamers`, `stream_history`); NEVER point it at `partner_*` views (service-role-only). `anonRestGetPaged` for >1000-row sets — PostgREST caps responses at 1000 rows, pass a deterministic `order`.
- **Quick facts** (`lib/server/quick-facts.ts`): the peak-viewer fact reads `stream_viewer_samples` — `stream_history.peak_viewer_count` is NULL across prod, do not switch back. The prediction fact is **HIGH-confidence, non-cancelled only** (UX round 2026-07-27: the all-confidence share sat near 50% and read as a coin flip; HIGH is the population the visible confidence badges promise, ~74% ±2h at the time) and hides below 20 evaluated rows/7d OR below 65% (`buildPredictionAccuracy` minTotal/minPct — a homepage trust signal, not a lab report). Cancelled slots are excluded because their evaluation is inverted (expiry without a stream = accurate), which the "started within two hours" copy wouldn't cover. The whole section hides below 2 cards.
- **Masthead is SIGNED-OUT ONLY** (rebuild round 2, 2026-07-27): `HomeMasthead` is nothing but the H1 (`hero.claim`, plain white — no brand-accent fragment) plus one conversion line, `<a>Log in</a> or <a>get the App</a> to follow your favorite streamers` (`hero.ctaLogin/ctaMid/ctaApp/ctaTail`; the sentence must START with the sign-in link in every language). The app half routes per device through `GetAppLink` (iOS → App Store, Android → Play, desktop + SSR → `/app`), the sign-in half through `signInGateRedirect('/feed')` and only while `AUTH_UI_VISIBLE` — otherwise the app-only variant (`ctaAppOnly*`) renders, so an auth-off build never shows a dead "Log in". `page.tsx` wraps the section in `<SignedOutOnly>`: SSR and the first client render always emit it (crawlers are anonymous and keep the H1), signed-in visitors drop it after mount and start at the section nav, keeping an `sr-only` H1 as the fallback so the document still has one. **The bottom gap belongs to the masthead (`mb-6`), NOT to the section nav** — with the nav owning it, the signed-in page opened on an empty strip. Dropped in this round and NOT to be reinstated without a product decision: the date/kicker overline, the live/soon ticker chip (`homeFeed.ticker`) and the store badges (they stay in `HomeEndCap`); `HomeSessionBanner` was deleted with the same round. `homeFeed.ticker` + `homeFeed.sessionBanner` remain translated but unrendered (flagged in `lib/i18n/hub/types.ts`).
- **Homepage metadata follows the claim, not the H1 wording**: title + description live in `SITE_META_STRINGS.home` for ALL 12 locales — `page.tsx` no longer duplicates the English pair inline (they were byte-identical and free to drift; `i18n-chrome.test.ts` pins them). Since the H1 dropped "Twitch & YouTube", the TITLE now carries that intent plus the three pillars ("Stream Schedule, Highlights & Stats — Twitch & YouTube | StreamerTimes"); keep both the platform names and the schedule intent in any future rewording — this is the page's actual query intent, the H1 is not. `og:title`/`og:description` (EN only; other locales reuse the page title/description), the WebSite JSON-LD description and the `app/[locale]/opengraph-image.tsx` card mirror the claim. That OG card stays English for every locale on purpose (Satori's default font is Latin-only — ja/ru/ar/uk would render tofu) and must live INSIDE the `[locale]` segment.
- **Sticky section nav** (`HomeSectionNav`, UX round 2026-07-27): chip row under the masthead, sticky below the header (`z-20`), jumping to `#home-live/-lineup/-trending/-clips/-stats/-discover` wrapper divs in page.tsx (each carries `SECTION_ANCHOR_CLASS` scroll-mt for the two sticky bars). The server builds the chip list by MIRRORING each section's own hide rule (lineup is unconditional — it has an empty state; `#home-stats` wraps Quick facts + Risers/Most-streamed + Most watched); keep those flags in sync when a section's hide rule changes. The component hides drift-orphaned chips at mount via the `hidden` attribute (offsetHeight probe — direct DOM like HomeUpNextFilters, no setState-in-effect) and runs the DayNavBar IntersectionObserver scroll-spy; the whole bar only renders with ≥3 chips.
- **Live rail = "Most Watched right now"** (section rebuild 2026-07-28, was "Live now"; `HomeLiveRail` + `HomeLiveRailFilters`, logic in `lib/home/live-rail.ts`, unit-tested): wide cards with the platform preview image, ranked by current viewers, two dropdowns (category + broadcast language) and a per-card runtime estimate. Invariants:
  - **A fresh viewer sample is required** (`pickBiggestLiveSlots`): the API nulls `viewer_count` after ~25 min, and slots stuck on `status='live'` are never re-sampled — two sat in prod on 2026-07-28, live for 6 and 12 days. Dropping unsampled slots is both the ranking precondition and the zombie guard (a dead slot renders a dead preview image). Below 4 sampled slots it falls back to the unfiltered pool, so a broken sampler degrades to the old behaviour instead of an empty section.
  - **The rendered pool is the WHOLE live sweep; the visible cut is only its head** (widened 2026-07-29 — before that the rail rendered 30 cards and the filters could not see past them, so "German" found the 3 German streams big enough for the top 30 while 14 others were live, and pt/it/pl had no option at all). `pickBiggestLiveSlots(slots, LIVE_RAIL_POOL_MAX)` — 200, a safety cap and NOT a target — feeds every card into the DOM; cards ranked past `LIVE_RAIL_DEFAULT_VISIBLE` (30) ship with `hidden` set SERVER-SIDE, so the no-JS/crawler view is the top cut and the island's first pass reproduces it byte-for-byte (`computeVisibleLiveIds` over an empty selection is that same prefix — no flash, no hydration mismatch). Selecting anything drops the cut and shows every match, uncapped. **The item order IS the rank order** (`buildLiveFilterItems` mirrors the slots) — a re-sort anywhere in that chain silently changes which cards are visible by default. **The scope note is gone** (2026-07-29, compactness): `liveFilterNote(top, total)` stays translated but unrendered — the dropdown options carry pool-wide counts ("German (17)") and the header links to /live, which states the scope without a third chrome line above the cards. If it ever comes back, `total` must be the POOL and deliberately not the header's `totalLive`: slots without a fresh viewer sample never entered the pool, so claiming to search them would be false. Hidden cards cost HTML + flight bytes and DOM elements, but NO image fetches (`hidden` is `display:none`, so their lazy thumbnails never intersect — verified over CDP: 133 rendered `<img>` tags, exactly 1 request). **Measured A/B at 133 live streamers** (same build, same data, Lighthouse mobile, 30 vs 133 rendered): score 89 → 88, TBT 24 → 73 ms, LCP 3711 → 3786 ms, transfer 594 → 615 KB, DOM 1190 → 3774 elements (~24 elements/card). Only the DOM-size audit changes character — it now exceeds Lighthouse's 1400-element threshold; everything else stays inside "good". DOM size is therefore the binding constraint, and the reason `LIVE_RAIL_POOL_MAX` exists at all: re-measure before raising it. The homepage sweep cursor-walks 2×500 (`LIVE_MAX_PAGES`) because simulcasts write one slot per platform and a single page would truncate silently at top-500 scale.
  - **Filter options are cross-filtered** (`countLiveFilterOptions` over the pool the OTHER dropdown narrowed) and a selection that stops being offered is clamped away in render (never a self-healing effect — `react-hooks/set-state-in-effect`). Together these make a zero-result combination unselectable; the empty state is a kept-but-unreachable backstop.
  - **`streamer_language` is a Partner-API field** added for this section (`toPublicSlot` + the `streamers!inner(...)` embed + `openapi.yaml`). NULL means unknown — notably every YouTube-only channel — and must never be defaulted to English; such slots simply get no language option. Distinct from `copy_language` (the AI copy's language). Language NAMES follow the viewer's locale via `languageDisplayName(code, locale)` (D6: chrome, not content).
  - **Runtime line**: `slot.duration_minutes` on a live slot is a real per-stream AI estimate (matched prediction → streamer average → 6 h default), so the line is approximate by construction — rounded to 5 min under 3 h and to whole hours above, always with a tilde. `is_always_on` OR `duration >= 100000` (the 525600 sentinel) renders the 24/7 badge instead; a lapsed estimate becomes "Longer than expected", never a negative countdown (28% of prod's non-always-on live slots were past their estimate). Strings live in `lib/i18n/live-runtime.ts` — a deliberately tiny CLIENT-SAFE lexicon, because the island re-derives every label each minute (ISR HTML is served stale and a tab ages arbitrarily) and cannot receive functions across the boundary. `HubLex` stays server-only; where the island needs a lexicon string it gets a `{label}`/`{count}` template, EXCEPT the match counter, which is pre-rendered per count so every language keeps its plural agreement.
  - **Cards link OUT to the platform** (2026-07-31, `liveWatchUrl`), unlike every other slot card on the site: `https://twitch.tv/<login>`, else `https://youtube.com/channel/<id>/live` (the `/live` suffix is what lands on the running stream), `target="_blank"` + the localized "opens in new tab" appended to the link's accessible name — site-wide convention for outbound watch links (`WatchPill`, `WatchButtons`). Everything in this rail is live by construction, so the intercepted `/schedule/<id>` modal only stood between the viewer and the stream they clicked. No SEO cost: robots.txt disallows `/schedule/` for search crawlers, so those were never indexable targets. Twitch wins a simulcast (its viewer count is what ranked the card); a slot with no channel id for any platform it claims falls back to the internal link rather than rendering a dead card.
- **Lineup**: featured-only (anon-REST id sweep, falls back to unfiltered on failure). **ONE grid, whole cards, no peek clamp** (2026-07-30, after two reported bugs): the server hands its cards to the island as an ARRAY of `<li>` so they share a single `<ul>` with the deferred ones, and cards past the reveal window are `hidden` (display:none → out of the flow). The old shape — two sibling `<ul>` grids, the second clamped to `max-h-24` — produced both symptoms: a half-filled row wherever the first grid ended on an odd visible count (measured rows 2,1,2,1), and a card cut at 77 px with the two rows behind it gone whenever every visible card sat in the clamped grid (filter a language absent from the top four, or serve a stale ISR page whose early predictions expired). Do NOT reintroduce a height clamp to tease the next row: the set of VISIBLE cards is not the set of RENDERED cards, so any clamp will eventually cut the first one. The reveal button carries that affordance. Pool = the whole 24 h window up to `LINEUP_POOL_MAX` (500 — a PAYLOAD guard, production holds ~440); the fetch cursor-walks 2×500 because the API orders by `start_time`, so a first-page cut is a cut in TIME and would amputate the evening from every filter.
  - **The pool is SPLIT: `LINEUP_SSR_COUNT` (24) cards are HTML, the rest travel as slot DATA** (`splitLineupSlots`) and are rendered by the island only when a filter is active or the list is expanded. Filter metadata covers the whole pool regardless, so the dropdown counts are honest from the first paint. Measured at 372 predictions with the live rail on the same page (Lighthouse mobile): all-HTML 12,859 DOM elements / 213 ms TBT / 3.0 s main-thread vs. the split at **4,482 / 41 ms / 1.4 s** — full coverage AND cheaper than the 120-card cap it replaced (6,783 / 72 ms), for the same transfer (646 vs 644 KB; the slot data costs about what the removed markup did). 24 is the section's pre-2026-07-30 render limit, so the crawlable surface is unchanged. **Two visibility regimes must not collide**: the imperative `hidden` pass selects `li[data-home-id]:not([data-home-deferred])`, because React owns the deferred nodes and renders only matches.
  - **Revealing is BATCHED and independent of filtering** (`lineupRevealLimit`, product call 2026-07-30): step 0 = `LINEUP_VISIBLE_COUNT` (4) whole cards, every click = one flat `LINEUP_REVEAL_STEP` (24) — 24 → 48 → 72 …, and the button says how many the NEXT click adds (`lineupShowMore`, so the last batch is honest). A dropdown NEVER opens the list: earlier versions auto-expanded "so a match isn't clipped", which meant one selection rendered the whole window. What a filter changes is the MATCH SET; the reveal window is a prefix of it (`matchingLineupIds` — order is load-bearing), so a filter whose matches all sit late in the day still shows its first four without expanding. The counter next to the dropdowns is what reports the full match count while the list stays collapsed. Clearing a filter does not collapse; "show less" (offered separately once revealed) returns to step 0 and drops the deferred tail again. `lineupShowAll` is now the island's fallback label only.
  - **Three dropdowns, same structure as the live rail** (2026-07-30, replaced six category chips): category + broadcast language + start time, logic in `lib/home/lineup-filters.ts` (unit-tested), option counting shared with the rail through `lib/home/filter-options.ts` (`countFilterOptions` — blank values skipped, count-desc/label ordering, one place for both sections). Cross-filtering, the render-time clamp of a no-longer-offered selection, and the pre-rendered-per-count match counter all follow the rail's contract. **Both sections' selects share `FILTER_SELECT_CLASS`** (`components/web/home/filter-controls.ts`, 2026-07-31) — keeping them in sync by comment had already cost the lineup its 44 px touch target when the chips landed after the mobile round. It carries `max-w-44 truncate … sm:max-w-60`, because a native select sizes itself to its WIDEST option: one long game name stretched the closed category pill to 382 px on a 390 px phone (the live rail's to 220 px), label floating far from the chevron. Only the CLOSED control elides; the popup list is native and unaffected.
  - **The time dimension is CUMULATIVE and LOCAL**: `LINEUP_TIME_HOURS` (14/16/18/20/22) filters "from this hour of the VIEWER's day onwards" — hour-of-day, deliberately not "every start after tonight 20:00", which would drag tomorrow morning in under an evening label. Options are count-suppressed when empty and kept CHRONOLOGICAL, not count-sorted (the counts are nested supersets, so count-desc would render the list backwards).
  - **Timezone → the clock must come from `useSyncExternalStore`.** One prerendered page serves every timezone, so anything derived from the local hour cannot be server-rendered: the first implementation baked "From 8:00 PM (36)" — counted in the BUILD machine's zone — straight into the HTML. `lib/home/minute-clock.ts` returns `null` as its server snapshot; the island renders that as "no time options, no expiry hiding" (exactly what the server can promise) and swaps in the real clock right after hydration. Verified over CDP with `Emulation.setTimezoneOverride`: Berlin 63/54/45/36/18, Tokyo 39/39/29/19/9, Los Angeles 10/1/1 (its evening options vanish — no seeded stream falls in those local hours), zero hydration warnings in all three. `react-hooks/set-state-in-effect` rules out the `useState`+`useEffect(setMounted)` shortcut; the rail's own island may keep its plain `useState(Date.now)` because its clock only ever drives DOM mutations, never render output.
  - Strings: the generic `liveFilter*` entries (category/language/option/reset + the "All" labels) are SHARED — they name a dimension, nothing live-specific. `lineupFilter*` covers what the rail's wording would get wrong: these are predictions, so the counter and empty state must not say "live" (pinned per locale in `i18n-hub.test.ts`). `homeFeed.chipAll` is now unrendered.
- **Most streamed this week**: `rankWeekStreamed` unions per-streamer vod intervals with 15-min gap tolerance — the pure-TS sibling of the SQL gaps-and-islands session dedupe (StreamHub CLAUDE.md "Session counting"); a simulcast writes 2 `stream_history` rows and Twitch splits one night into several VODs, so never count raw rows. Filter `source=eq.vod` (`stream_slot` rows are synthetic always-on fragments).
- **Trending rail**: `HomeTrendingRail` joins the trending list to the games catalog BY CATEGORY NAME and carries the /games card info block (top-streamer name links on `relative z-10` beside the stretched card link — never nested anchors); games missing from the catalog render a plain box-art tile without links. `components/web/games/TrendingRail` is untouched and still serves `GamesHubView`; `TrendBadge` is exported from `GameCard`.
- **Discover grid**: 6 RANDOM featured streamers (`sampleRandom`, rotates per ISR regeneration) each with next stream + category — oversample 12, resolve via the category-agnostic `getNextSlotByStreamer`, then `preferWithNextSlot` so cards that can actually show a next stream win the visible spots; `slot_kind='cancelled'` suppresses the line. Featured-fetch failure falls back to the popular list. The interrupt card + `PopularStreamersFooter` keep the popular list on purpose (the footer is the crawlable SEO pill list).
- **Login CTAs follow `AUTH_UI_VISIBLE`** (masthead CTA, upsell sheet, end-cap web alternative, interrupt card) — they hide themselves (masthead: swap to the app-only sentence) while auth is dormant or in stealth mode.
- All copy lives in `HubLex.homeFeed` + `HubLex.hero` (12 languages, completeness-tested). Known follow-ups: browser QA of the remaining client islands (chips, upsell sheet, clip lightbox — the masthead gate was verified headless in both session states), GA4 CTA events (no gtag helper exists yet), and "Risers" hides itself until fastest-growing snapshots mature. Open UX question: the interrupt card still offers "Sign in on the web" to visitors who ARE signed in.

# Mobile UX round (2026-07-30)

A mobile audit at 390px found the same three defect families across many pages. These are the rules that came out of it — check them when touching any list, card row or overlay control.

- **`min-w-0` belongs on the GRID/FLEX ITEM, not just inside the row.** A grid item defaults to `min-width:auto`, so a `truncate` descendant stretches the whole track: `/live` was scrolling sideways at 1031px on a 390px viewport, and titles in "Recent streams" were cut mid-word because the ellipsis container was wider than the screen. An inner `min-w-0 flex-1` cannot fix that — the track already grew. Every `<li>` in a grid of cards needs `min-w-0`, and so does every flex row that pairs a `truncate` label with a badge.
- **Badges are `shrink-0`** (`components/web/Badges.tsx`). Flexbox otherwise shrinks the short unwrappable word (LIVE, Twitch) instead of the label that has somewhere to go. Badge font floor is 10px; the dense `sm` scale used to be 9px.
- **Touch targets: 44px, without moving the design.** Small overlay controls (favourite heart 32px, lineup bell 28px, header buttons 36px) keep their visual box and gain an invisible `::before` hit area via `touchTargetExpander` in `lib/ui/positioning.ts`. That helper only adds `relative` when the caller has NOT positioned the control: Tailwind emits all `position` utilities in one layer, so a blanket `relative` beat callers' `absolute` (equal specificity, stylesheet order decides) and overlay hearts/bells silently fell out of their corners. Chips that stand alone in a row grow for real (`min-h-11`) — the pseudo-element trick would make neighbours' hit areas overlap across an 8px gap. `--header-height` is a layout contract for every sticky offset: the header must never grow, so its controls always use the expander.
- **Day labels are decided against the VIEWER's date, not the server's UTC date.** The schedule is grouped into UTC days; between local midnight and UTC midnight "today UTC" and the reader's own today disagree, and the page used to label a section "Tomorrow" while the slot rows inside it said "Thu, Jul 30 · around 7pm your time". `components/web/DayLabel.tsx` renders the heading via `useSyncExternalStore` (server snapshot = the UTC-referenced string, so ISR HTML stays deterministic; client snapshot recomputes against `localDateKey()`). aria labels use `utcDateAbsoluteLabel` — never the relative word, which would contradict the hydrated heading.
- **`LocalTime` splits the clock from the zone name**: hour/minute follow the page language, the abbreviation is probed via `en-US`/`en-GB` and stays latinized. Passing `undefined` as the locale took the abbreviation from the BROWSER's locale, so a German-configured browser rendered "8:06 PM MESZ" on the English page.
- **Ranking tables freeze the primary metric** (`PRIMARY_STICKY` in `RankingTable.tsx`). Six columns cannot fit a 390px phone at any width, and the ranked-by number was the column falling off the right edge — "39.7K" read as "39.7", a wrong number rather than a truncated one. Scrollbars stay visible on data tables (they are the scroll affordance) and hidden on decorative chip rails.
- **`/live` carries a `<details>` jump index** (`components/web/live/LiveJumpNav.tsx`) because the page runs to dozens of category sections and ~24,000px. Chips are server-rendered anchors (works with JS off); the client island exists only to close the panel on tap, since a sticky open panel would cover the heading it just scrolled to. Sections need `scroll-mt-32` to clear header + jump bar.
- **The locale banner hides its sentence below `sm`** instead of truncating it: at 390px there is room for ~20 characters next to the CTA, and a cut-off word says less than the CTA already does. The full sentence remains the region's `aria-label`.
- Chrome that must stay locale-aware: `HeaderUserMenu` (label + `localeHref`), `FloatingGetAppButton` (label + `localeHref`, and its hide-on-`/auth` check runs through `stripLocaleFromPath` — plain `startsWith('/auth')` only matched the English tree). The game hub is deliberately left English-only until M22 P4 localizes it as one piece; do not half-localize it.

# Release 2026-07-30 — four branches merged together

`main` had lagged four unmerged feature branches. They went to production as one release (`e41a6e1..7458a00`), in this order: `feature/bing-seo-round` → `feature/hide-interrupt-card-when-signed-in` → `feature/live-rail-full-pool` → `feature/mobile-ux-round`. Verified on the integrated tree before the push: `tsc --noEmit` clean, eslint 0 errors (4 pre-existing warnings), 973 unit tests, `next build`, and a mobile browser pass at 390px.

Two conflicts, both because the live-rail branch and the mobile round reworked the same homepage surfaces. Worth knowing if you touch either again:

- The lineup's category CHIPS became three selects (`HomeUpNextFilters`), so the mobile round's `chipClass` had nothing left to style. Its intent — 44px touch targets — moved onto `selectClass` and the reset button. **When a control changes shape, carry the touch-target rule across rather than dropping it with the old class.**
- `app/[locale]/live/page.tsx` is treated as BINARY by git (no NUL bytes, no `.gitattributes` — the heuristic simply trips on it), so a conflict there produces **no conflict markers** and silently leaves "ours" in the tree. Resolve it by diffing the three merge stages (`git show :1: / :2: / :3:`) instead of looking for markers. Miss that and one side's change vanishes without a trace.

After merging, re-check that auto-merged files kept the mobile round's intent — auto-merge can succeed textually and still lose it. The markers to grep for: `reserveTopRight`, `liveCategories`, `touchTargetExpander`, `DayLabel`, `min-h-11`.

# Performance baseline (M20 Phase 0)

Web-Vitals tooling for the perf-health runbook (`StreamHub/docs/performance-health.md`):

- **`npm run lighthouse`** → `scripts/lighthouse-baseline.mjs`: runs the local `lighthouse` CLI over the URLs in `lighthouserc.json` (3 mobile runs each), prints median LCP/TBT/CLS/score + LCP element. **Windows-robust**: it writes each LHR before Chrome's teardown (chrome-launcher throws `EPERM` on Windows temp-cleanup *after* the report generates, which makes a bare `lhci autorun` abort with no output). `LH_RUNS=1` for a quick smoke run.
- **`npm run lighthouse:ci`** → `lhci autorun` (Linux/CI only — the teardown bug doesn't fire there) — also enforces the budget assertions in `lighthouserc.json`. This is the hook for a future Lighthouse-CI budget (M20 S5.2).
- Baseline finding to remember: streamer-page LCP is **render-delay-bound**, the LCP element is a text `<p>`, **not** the hero avatar — so adding `priority` to the avatar is a non-lever for LCP. Re-diagnose via the `largest-contentful-paint-element` audit before any LCP work.
- **`/streamers` pagination (M20 S1.5, 5k-scale):** `components/web/StreamersIndexView.tsx` fetches only its own 60 streamers via the partner API's `?offset=` param (returns exact `pagination.total`) — **do NOT revert to fetch-all-slice** (that reintroduced a 2,000-streamer cap). Routes: `/streamers` (page 1) + `/streamers/page/[n]` (2+), self-canonical per page, `/page/1` + out-of-range → 404, soft-404 rule preserved. The cross-page A–Z jump bar was intentionally dropped (can't map letters→pages without the full roster); in-page letter headers + numeric nav remain. If you ever want the jump bar back, add a letter-histogram endpoint — don't fetch the whole roster.
- Schedule-page LCP diagnosis (M20 S1.6, 2026-07-13): **live-slot pages** (`thumbnail_url` set) have the hero thumbnail as LCP element — it now carries `priority`. **Prediction-slot pages** (no thumbnail) have a text `<p>` LCP like the streamer page; their avatar-fallback hero deliberately stays lazy.
- **Every dynamic route that should be ISR-cached MUST export `generateStaticParams`** (an empty array is fine). Without it, Next builds the route as `ƒ` (per-request dynamic) and never caches the HTML — `export const revalidate` alone does nothing for the route cache. This bit `/schedule/[id]` until M20 S1.6 (every visitor paid a full server render, `Cache-Control: private, no-store`); `/streamer/[slug]` documents the same pattern. Check the build output: the route must show `●`, not `ƒ`.

# Repo hygiene (added 2026-07-07 after a stale-branch cleanup)

- `main` is the single source of truth and the GitHub default branch; it is protected against force-pushes and deletion. Vercel deploys production from `main`.
- Merge finished, verified feature branches into main promptly, then delete the branch local + remote (`git branch -d` + `git push origin --delete`). GitHub auto-deletes head branches of merged PRs (`delete_branch_on_merge` is ON).
- If a branch's content reached main another way (cherry-pick/re-implementation), verify with `git cherry main <branch>` and delete it.
- Remove git worktrees when their story is done (`git worktree remove`); never park uncommitted work in a worktree.
- Doc-only or config-only tweaks may be committed directly on main.

# Auth activation flag (`NEXT_PUBLIC_AUTH_ENABLED`)

Website auth ships dormant and is activated WITHOUT code changes by setting the Vercel env var `NEXT_PUBLIC_AUTH_ENABLED=true` and redeploying (the value is inlined at build time — a runtime toggle is impossible by design, which is what keeps the static root layout static). Single source of truth: `lib/auth-flag.ts` (`AUTH_ENABLED`, `safeNextPath`, `signInGateRedirect`).

Flag OFF (default) — exact dormant behavior: `/auth/login` redirects to `/app`, no user menu in the header, gated pages (`/feed`, `/feed/interests`, `/program`, `/favorites`, `/settings`, `/onboarding`) redirect signed-out visitors to `/app`, FavoriteButton's signed-out state points to `/app?from=favorite`. The fresh-signup redirect into `/onboarding` is also disabled while dormant (`postAuthDestination` takes `authEnabled`).

Flag ON: `/auth/login` renders the Twitch/Google sign-in UI, the header mounts `HeaderUserMenu` (Sign-in link → avatar dropdown with My feed / Program / My favorites / Settings / Sign out), gated pages redirect to `/auth/login?next=<page>` and return there after login (the `next` path survives the OAuth round-trip via the callback's `?next=` param), FavoriteButton's signed-out state leads to sign-in with a return path. `next` values are sanitized against open redirects (`safeNextPath`: same-site absolute paths only) in the login page, the server actions, and the callback route.

**Activation checklist (in order):**
1. Supabase dashboard: Twitch + Google OAuth providers configured, `https://streamertimes.tv/auth/callback` AND `https://streamertimes.tv/auth/callback?next=*` in the redirect allowlist (the second entry covers the gated-page return path — `next` is URL-encoded so `*` matches it; GoTrue matches query strings literally), site URL correct.
1b. Twitch dev console (for the follows import — separate from Supabase): add `https://streamertimes.tv/auth/twitch-import` (+ `http://localhost:3000/auth/twitch-import` for local) to the app's OAuth Redirect URLs, and set `NEXT_PUBLIC_TWITCH_CLIENT_ID` on Vercel (same value as the backend `TWITCH_CLIENT_ID` secret). Without this the import buttons fall back to "pick manually".
2. Vercel: set `NEXT_PUBLIC_AUTH_ENABLED=true` → redeploy.
3. Smoke-test: `/auth/login` renders; complete one Twitch and one Google login; `/feed` loads and `st_feed_seen`/`feed_events` behave; sign-out works.
4. Rollback = unset the env var + redeploy (fully reversible, no data impact).

**Stealth mode (`NEXT_PUBLIC_AUTH_LINKS_HIDDEN=true`)** — for quiet production testing on top of `NEXT_PUBLIC_AUTH_ENABLED=true` (no effect while auth is dormant): auth is fully functional via direct URL (`/auth/login` renders, gated pages redirect to sign-in, OAuth round-trip works), but signed-OUT visitors see no entry point — the header renders no "Sign in" link (`HeaderUserMenu` returns null when signed out) and FavoriteButton's signed-out state keeps the dormant `/app?from=favorite` behavior. Signed-IN UI is unaffected: after logging in the header shows the avatar dropdown (My feed / Program / My favorites / Settings / Sign out). Source: `AUTH_UI_VISIBLE` in `lib/auth-flag.ts`. Go fully public by unsetting the var + redeploy.

# Email auth sub-flag (`NEXT_PUBLIC_EMAIL_AUTH_ENABLED`)

Email login/registration ships dormant on top of the base auth flag: `EMAIL_AUTH_ENABLED` (lib/auth-flag.ts) is only true when BOTH `NEXT_PUBLIC_AUTH_ENABLED=true` AND `NEXT_PUBLIC_EMAIL_AUTH_ENABLED=true`. This lets OAuth-only auth go live first and email auth follow (or roll back) independently.

Sub-flag OFF: `/auth/login` shows only the OAuth buttons; `/auth/sign-up`, `/auth/forgot-password`, `/auth/reset-password` redirect to `/auth/login` (base auth on) or `/app` (everything off) via `emailAuthGateRedirect()`. Sub-flag ON: `/auth/login` additionally renders the email/password form (`components/web/auth/EmailLoginForm`) with sign-up + forgot-password links, and the three pages render their forms. `app/auth/confirm/route.ts` is deliberately NOT flag-gated: in-flight confirmation/recovery email links must keep working even if the sub-flag is rolled back mid-flight (with a bogus/expired token it only ever redirects to an error page).

Key mechanics (don't break these):
- **hCaptcha is mandatory in prod**: the Supabase project enforces captcha on auth endpoints (same as the mobile app). All email-auth calls go through the browser Supabase client from client components, with a token from `components/web/auth/HCaptchaField` (invisible mode, `NEXT_PUBLIC_HCAPTCHA_SITE_KEY`). When the sitekey env is unset the forms skip captcha — only valid against a captcha-free stack. Local dev: hCaptcha test sitekey `10000000-ffff-ffff-ffff-000000000001` + start the local Supabase stack with `HCAPTCHA_SECRET=0x0000000000000000000000000000000000000000` (config.toml reads `env(HCAPTCHA_SECRET)`).
- **Confirmation/recovery links use `token_hash` + `/auth/confirm` (verifyOtp)**, NOT the `?code=` PKCE exchange — this makes cross-device clicks work (sign up on desktop, confirm on phone). It depends on the CONDITIONAL Supabase email templates ("Confirm signup" / "Reset password") that branch on the exact `{{ .RedirectTo }}` strings built by `lib/auth-email.ts` (`signupEmailRedirectTo`, `recoveryRedirectTo` — unit-frozen in `lib/__tests__/auth-email.test.ts`). The templates' `else` branch keeps `{{ .ConfirmationURL }}` for the mobile app — NEVER remove it, and never change the redirect strings without updating the templates in the Supabase dashboard. Without the template change (e.g. local dev defaults), website links fall back to the same-browser `?code=` flow through `/auth/callback`.
- Auth emails are sent from `noreply@streamertimes.info` via Resend custom SMTP (Supabase dashboard → Auth → SMTP); the domain is verified in Resend (SPF/DKIM). The default Supabase mailer is limited to ~2 emails/hour — never rely on it in prod.

**Activation checklist (in order; base auth flag must already be live):**
1. Resend: `streamertimes.info` domain verified (DKIM/SPF DNS records); sending API key created.
2. Supabase dashboard → Auth → SMTP: `smtp.resend.com:465`, user `resend`, pass = API key, sender `noreply@streamertimes.info` / "Streamer Times". Bump Auth rate limit "emails per hour" (2 → ~60). Smoke-test a password reset from the MOBILE app (sender changes for the app too).
3. Supabase dashboard → Auth → URL Configuration: add `https://streamertimes.tv/auth/callback?next=/auth/reset-password` to the redirect allowlist (`…/auth/callback` should already be there). Do NOT change the Site URL — the app's signup flow depends on it.
4. Supabase dashboard → Auth → Emails: snapshot the current "Confirm signup" + "Reset password" bodies, then wrap them in the conditional (`{{ if eq .RedirectTo "https://streamertimes.tv/auth/callback" }}` → confirm link `https://streamertimes.tv/auth/confirm?token_hash={{ .TokenHash }}&type=signup`; recovery: match `https://streamertimes.tv/auth/callback?next=/auth/reset-password` → `…&type=recovery`; `else` = snapshot). After EACH template save, re-test the matching mobile-app flow.
5. hCaptcha dashboard: add `streamertimes.tv` to the allowed hostnames of the app's sitekey.
6. Vercel: set `NEXT_PUBLIC_HCAPTCHA_SITE_KEY=<sitekey>` and `NEXT_PUBLIC_EMAIL_AUTH_ENABLED=true` → redeploy.
7. Smoke-test: sign-up with a scratch email on desktop, confirm ON A PHONE (cross-device proof) → signed in; wrong-password error copy; forgot → reset roundtrip; one Twitch + one Google login (regression); mobile-app signup + reset with a scratch account (template regression); mail headers show DKIM pass via Resend.
8. Rollback = unset `NEXT_PUBLIC_EMAIL_AUTH_ENABLED` + redeploy. Steps 1–5 can stay in place harmlessly.

# Middleware session-refresh budget (2026-07-28)

`updateSession()` (`lib/supabase/middleware-helper.ts`) runs in front of EVERY request, so its failure mode is site-wide, not auth-local. On 2026-07-26/27/28 a swapping Supabase instance made GoTrue answer in tens of seconds and Vercel killed the edge middleware — **504 `MIDDLEWARE_INVOCATION_TIMEOUT` on ordinary pages, for logged-in visitors only**. Invariants that keep that from recurring:

- **Anonymous requests must never touch Supabase.** The `sb-*` cookie prefix check returns before the client is constructed. All SEO traffic depends on this — it is why anonymous visitors saw a healthy site throughout the incident. Do not move work above that check.
- **The refresh is bounded by `SESSION_REFRESH_BUDGET_MS` (2.5s).** A healthy round trip measured ~100ms from Vercel (A/B: anon median 61ms vs logged-in 160ms on `/live`), so the budget only fires when the backend is genuinely unwell.
- **One `AbortController` drives both halves, and both are required.** As a signal it cancels the in-flight GoTrue request; as a race participant it guarantees return. The signal ALONE is not enough: auth-js wraps an aborted fetch into a *retryable* error and keeps retrying with exponential backoff until `AUTO_REFRESH_TICK_DURATION_MS` (30s) — past the middleware limit. Never replace the race with "just an AbortSignal".
- **A timeout degrades, it does not log anyone out.** The existing cookies are forwarded untouched and the refresh retries on the next request. A `setAll` arriving after the budget cannot corrupt the returned response (it reassigns the local variable to a NEW `NextResponse`).
- **This does not rescue gated pages** — `/feed`, `/favorites`, `/program`, `/settings`, `/onboarding` and `/auth/*` call `getUser()` themselves and would still stall. Everything else (`/`, `/streamer/*`, `/game/*`, `/rankings/*`, `/live`) has no server-side auth call at all, so for those the middleware is the only Supabase touchpoint and bounding it is sufficient.

Frozen by `lib/supabase/__tests__/middleware-helper.test.ts`. Root cause of the incident was compute size, fixed separately (Micro → Small, 2026-07-28); the budget is the tier-independent guard.

# SEO surface

Marketing/hub SEO conventions (last extended 2026-07-12 — keywords cleanup, global 404, llms.txt, per-hub OG images):

- **Metadata**: no `keywords` meta anywhere (Google ignores it; removed from the root layout). `metadataBase` is set ONCE in `app/layout.tsx`; every page owns its `title`/`description`/`alternates.canonical`/`openGraph`. Intent split: the homepage keeps the "Stream Guide" wording, `/app` targets download/alert intent ("Go-Live Alerts & AI Stream Predictions") so the two don't cannibalize.
- **OpenGraph images** (file-based `opengraph-image.tsx` per route segment). **They attach ONLY to pages in their OWN segment — there is NO nearest-ancestor inheritance** (verified against prod 2026-07-27: the hub image at `app/[locale]/rankings/opengraph-image.tsx` never reached `/rankings/most-followed`, and the old root `app/opengraph-image.tsx` stopped reaching the homepage when M22 moved pages under `app/[locale]/`). Every page that should have a card needs a file in its own folder; the brand card for the homepage lives at `app/[locale]/opengraph-image.tsx` (+ `twitter-image.tsx`), the five leaderboards share `lib/og/leaderboard.tsx` via thin per-folder wrappers:
  - Shared branded frame in `lib/og/frame.tsx` (`renderOgFrame({ title, subtitle, eyebrow?, pills? })` + `OG_SIZE`) — dark canvas, cyan radial glow, cyan/magenta corner brackets, "Streamer Times" eyebrow, STREAMERTIMES.TV footer. Reuse it for any new OG image; declare `runtime`/`size`/`alt`/`contentType` inline per route (Next reads them per segment).
  - Hub images are DYNAMIC (`app/live`, `app/games`, `app/streamers`, `app/game/[slug]`): `runtime = 'nodejs'` (so `PARTNER_API_KEY` reaches the route in `next dev`), one cheap Partner API call for a counter/title, `revalidate = 300`. **Every fetch — including the `getPartnerApi()` call itself, which throws when the key is unset — is wrapped in `try/catch` and degrades to a count-free fallback. Never throw: a throw during prerender aborts the whole production build** (documented incident 2026-07-07). The Partner API exposes no total-row count (`PaginationInfo = {next_cursor, has_more}`), so `/live` and `/streamers` show the 60s-cached live-now count (`getLiveStreamerIdSet`), `/games` counts `listGames` rows, and `/game/[slug]` shows the resolved category name (slug prettified on failure).
  - The root `app/opengraph-image.tsx`/`twitter-image.tsx` were REMOVED 2026-07-27 (orphaned by the M22 restructure); their `/opengraph-image` + `/twitter-image` URLs now resolve through the normal `/en` middleware rewrite to the `app/[locale]/` brand images (middleware PASSTHROUGH entries dropped too).
- **Global 404**: `app/[locale]/not-found.tsx` (localized, links to Home + hubs) renders for `notFound()` throws — but in Next 16 unmatched URLs ONLY hit a ROOT `app/not-found.tsx`, which cannot exist here (the root layout lives inside `[locale]`; `global-not-found.js` is still experimental). The catch-all `app/[locale]/[...notFound]/page.tsx` closes that gap: it claims every URL no real route matched (after the middleware's locale rewrite) and throws `notFound()`, so unknown URLs get the branded 404 inside the site chrome with a real 404 status. Deliberately no `generateStaticParams` (unbounded junk-URL param space — don't ISR-cache bot scans). Residual gap, accepted: dotted paths (`/wp-login.php`) bypass the locale rewrite and keep the unbranded framework 404.
- **`/game/[slug]` section budget** (2026-07-20): the page is a hub, not a listing — every section above the schedule/related/FAQ blocks is capped (`LIVE_DISPLAY_LIMIT = 4` live slots by viewer count, `RANK_DISPLAY_LIMIT = 5` ranking rows; depth lives on `/rankings/game/[slug]`, linked below the table). The roster grid renders ONLY when `ranked.length === 0` (categories with no follower data) — never alongside the ranking, it duplicated the same streamers. **The ItemList JSON-LD must mirror what is actually rendered** (ranking when present, otherwise the roster): listing entities the page doesn't show is a structured-data mismatch. Raising a display limit means re-checking the ItemList branch.
- **`public/llms.txt`**: served at `/llms.txt`, a curated Markdown map (USP + core URLs + Partner API) for LLM crawlers. Keep its URL list in sync with `app/sitemap.ts` `STATIC_URLS`.
- **`/schedule/*` slot pages** (decision 2026-07-15): noindex + robots.txt disallow **for Googlebot only** + 308 to `/streamer/[slug]` on expired `ai_slot_pred_*` ids (`lib/prediction-redirect.ts`). The ids churn every prediction cycle, so GSC listing them under "Page with redirect" / "Blocked by robots.txt" is EXPECTED and neutral — never start a GSC validation for those buckets (it fails by design). The `*` robots group deliberately does NOT block `/schedule/`: Discordbot/Twitterbot respect robots.txt and would stop rendering embeds of user-shared slot URLs (structure frozen by `app/__tests__/robots.test.ts`).
- **Auth-gated routes in robots.txt**: `/settings`, `/favorites`, `/feed` (added 2026-07-15; prefix also covers `/feed/interests`), `/program` (2026-07-22), and `/onboarding` (2026-07-22) are disallowed for ALL bots — they 307 every anonymous crawler (to `/app` while auth is dormant, to the login page once enabled), so their pre-render `noindex` metadata never takes effect and any crawl would only log a redirect. Keep new login-gated routes in this list.
- **OG image URLs in GSC** (`/streamer/[slug]/opengraph-image?<hash>` etc.): appear under "Crawled – currently not indexed" because they serve a PNG, not HTML; the `?hash` changes per deploy, so entries accumulate. Harmless and REQUIRED to stay crawlable — never robots-block or noindex them, or Discord/Twitter/Google previews of streamer pages break.
- **`/rankings` leaderboards** (added 2026-07-17): hub + four Top-100 pages (`most-followed`, `most-watched`, `most-active`, `most-reliable`) fed by `GET /v1/rankings/{metric}` (`getRankings`), plus depth-50 per-game rankings at `/rankings/game/[slug]`. Index gate: **< 10 sanitized entries → `robots: noindex,follow`** (`isRankingIndexable` in `lib/rankings.ts`) — pages always render (empty state, never 404) and flip indexable as data accrues (most-reliable launches thin by design). The sitemap mirrors the per-game gate via the cheap `streamer_count >= 10` proxy; the four leaderboard URLs are static sitemap entries. One shared OG image at `app/rankings/opengraph-image.tsx` (subpages inherit). Copy/columns live in the `RANKING_PAGES` registry (`lib/rankings.ts`, unit-tested); tables render via `components/web/RankingTable.tsx`. Titles degrade honestly with entry count — never claim "Top 100" with fewer rows. SEO/UI pass 2026-07-19: visible "Data refreshed" line from the envelope's `refreshed_at` (null-safe, hidden for table-backed metrics), ItemList Persons carry the streamer pages' `{url}#person` `@id` + follower `interactionStatistic` (entity consolidation), per-metric FAQ blocks in the registry (`faq` — plain content, deliberately NO FAQPage JSON-LD: rich results restricted to gov/health since 2023), sibling-metric tab nav above each leaderboard, medal accents + `#rank-{n}` row anchors (`rowAnchorPrefix` prop — leave unset on multi-table pages like the hub, ids would collide), "—" footnote via `hasMissingValues`, and hub game chips link every game clearing the `streamer_count >= 10` gate (cap 24, popularity fallback while nothing clears it) with matching card links on `/games`. No sticky table header: the sticky site header + the table's `overflow-x-auto` wrapper break page-scroll `position: sticky`. **Rankings expansion 2026-07-20** (backend: StreamHub `feat(rankings)` — `game_streamer_stats`, `ranking_snapshots` + 05:05 UTC capture cron, Partner API `category_stats` / `values.previous_rank` / `related_categories`): the four leaderboards + hub run `revalidate = 300` and render LIVE badges (`RankingTable liveIds` prop — pass ONLY from pages with ≤300 s revalidate), a "Next stream" column (`lib/server/next-streams.ts` fetch + `lib/next-stream.ts` pure helpers + `NextStreamTime` client component: UTC label SSR, browser-local after hydration, `~` prefix = AI-predicted), a `RankingSpotlight` #1 hero card, and ▲▼/new **7d trend arrows** (`rankTrend()` from `values.previous_rank`; the API omits the key until snapshots are ≥6 days old — arrows appear ~2026-07-27, no website change needed). `/rankings/game/[slug]` is a full page now: box-art header + stats chips (live numbers derived from the SAME slot fetch as the row badges — never mix with the 1h-cached `game.live_*`), interactive `GameRankingExplorer` (client: sort toggle followers/hours/viewers, language chips via `languageKey`/`languageDisplayName`, canonical rank always displayed; anchors + trend arrows only in the canonical follower view), data-driven FAQ (`buildGameRankingFaq`), "Related rankings" chips (`game.related_categories`, validated against the catalog before linking), VideoGame JSON-LD (same `isVideoGameCategory` gate as the game hub), follower-freshness line, `revalidate = 300`. View-model logic lives in `lib/game-ranking.ts` (serializable `GameRankingRow` — the explorer is a client component, no functions cross the boundary), unit-tested in `lib/__tests__/game-ranking.test.ts`. **Fastest-growing + main-game links 2026-07-20** (backend: `follower_snapshots` daily counts of ALL visible streamers via the same 05:05 UTC capture cron, `partner_rankings_fastest_growing` view, `top_category` on every rankings entry): fifth leaderboard `/rankings/fastest-growing` (`values.follower_gain_7d` primary, `formatSignedCompact`/`formatGrowthPercent` in `lib/rankings.ts`) — **empty + noindex until the first ≥7-day-old snapshot (~2026-07-27)**, the hub hides the section and its intro counts leaderboards dynamically; all Top-100 pages append a "Main game" column from `entry.top_category`, linked to `/rankings/game/[slug]` only when the category is in the games list (`mainGameSlugs` map — the route 404s outside it; plain text otherwise). **Climbers recap 2026-07-27**: `/rankings/climbers` ("Biggest climbers this week") is the weekly-movers recap built from `values.previous_rank` — per-metric climber tables + "new in the top 100" lines for most-followed/most-watched/most-active (pure logic in `lib/rankings-climbers.ts`, unit-tested; loader `lib/server/climbers.ts` shares the leaderboards' data cache), plus a growth spotlight from fastest-growing. `previous_rank` semantics guard: ABSENT = warming up (never a mover), `null` on a current-top-100 row = "new in the top 100" (a presence claim — never "grew"). Index gate `MIN_INDEXABLE_MOVERS = 10` across sections; English body + en-only indexable like the leaderboards; linked from the hub footer (lexicon key `rankings.climbersThisWeek` ×12), the leaderboard tab nav + sibling footers, sitemap and llms.txt.

# Localization (M22)

The D6 keying rule (applies to EVERY component and metadata builder):

- **UI/chrome strings follow the VIEWER's locale** — the `[locale]` route param, threaded as a prop or resolved from `params`. NEVER from cookies or `Accept-Language` (the `NEXT_LOCALE` cookie is client-only: banner + switcher highlight; a server read would fragment the CDN cache and break ISR — K1 class).
- **Content (bio, slot reasoning) follows the best available content language for that viewer**, always rendered with its OWN `lang`/`dir` attributes: `pickDescription(streamer, viewerLocale)` in `lib/seo.ts` (original on the streamer's own-language variant, `description_en ?? description` everywhere else) and `pickReasoning(slot, viewerLanguage)` in `lib/slot-copy.ts` (real copy when its `copy_language` is the viewer's or English; otherwise the always-English `generic_reasoning`, labelled via `SlotLex.autoSummary`).

Lexicon inventory (typed `Record<UiLang, …>` — a missing key/language is a compile error; every table has a vitest completeness + English byte-identity test): `lib/i18n-ui.ts` (streamer-page body, per-lang files `lib/i18n/ui/*.ts`), `lib/i18n-slot.ts` (client-safe slot chrome), `lib/i18n-chrome.ts` (header/footer/404), `lib/i18n-sitemeta.ts` (hub titles/descriptions), `lib/i18n-hub.ts` (hub BODY copy, per-lang files `lib/i18n/hub/*.ts`), `lib/i18n-banner.ts` (locale suggestion banner). English values are byte-identical to the pre-M22 inline strings — en pages must never change when a string moves into a lexicon.

Indexability matrix (P3, implemented in `applyLocaleSeo(meta, locale, path, indexableLocales)` — ALWAYS the last call in a `generateMetadata`):

- Hubs `/`, `/live`, `/streamers` (+ `/streamers/page/[n]`), `/games`, `/rankings`: `INDEXABLE_HUB_LOCALES` (`lib/seo.ts`, currently `['en','de']`; Phase 4 widens it — one-line change, metadata + sitemap follow).
- `/streamer/[slug]`: `streamerIndexableLocales(streamer.language)` — en + own language when it's one of the 12; the thin-page gate inside `buildStreamerMetadata` wins for every variant (incoming `noindex` is never overridden and never gets a cluster).
- Everything else (game pages, leaderboards, games/[view], legal, /app, /developers): en-only (omit the 4th arg). Non-en variants are viewable but `noindex,follow` + self-canonical — NEVER a foreign canonical next to noindex.
- hreflang clusters (`alternates.languages` + `x-default` → unprefixed en) are emitted ONLY on indexable variants and list ONLY indexable variants; `app/sitemap.ts` mirrors them (one entry per indexable variant, each carrying the full cluster). A cluster needs ≥2 members or it is omitted entirely.
- No server-side locale redirects, ever (cloaking risk). `/en/*` → 308 unprefixed is the single exception (URL normalization, not negotiation).

GSC monitoring after any locale/index change (first review ~2 weeks post-deploy): (1) Settings → International Targeting / hreflang errors must stay at zero ("no return tags" = a cluster listing a noindexed variant); (2) Coverage: the /de hub pair + own-language streamer URLs should move to "Indexed" without a matching drop of their en twins (duplicate-cluster = matrix bug); (3) soft-404/duplicate spikes on `/de/*` mean body localization is too thin — pull the locale from `INDEXABLE_HUB_LOCALES` rather than shipping half-translated indexable pages; (4) URL-inspect one en+de hub pair and one en+own-language streamer pair for "Google-selected canonical = user-declared".
